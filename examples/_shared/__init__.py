"""Shared helpers for the vstack examples gallery.

The :mod:`examples._shared.traces` module exposes synthetic agent
traces used by multiple example scripts; reusing them keeps each demo
focused on the pattern logic rather than trace boilerplate.

The helpers are NOT exported from the ``valanistack`` wheel — they
live alongside the examples in the source repo only.
"""

from __future__ import annotations
